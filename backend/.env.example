# Copy this file to .env and fill in your values.

# PostgreSQL connection string (async asyncpg driver)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/vendorhub

# Set to "true" to log every SQL statement to stdout (useful for debugging)
SQL_ECHO=false
